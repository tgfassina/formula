{
  "name": "backend",
  "private": true,
  "main": "index.js",
  "scripts": {
    "fixpack": "fixpack",
    "start": "nodemon $npm_package_main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.16.1",
    "express": "^4.14.1"
  },
  "devDependencies": {
    "fixpack": "^2.3.1",
    "nodemon": "^1.11.0"
  }
}
